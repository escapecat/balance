# 放到手机上

**已经部署好了:https://escapecat.github.io/balance/**

手机浏览器打开 → 加到主屏幕。之后有图标、全屏、没有地址栏,离线也能开。

- iPhone:**Safari** 打开(Chrome 不行,iOS 只有 Safari 能装)→ 分享 → 添加到主屏幕
- 安卓:浏览器菜单 → 添加到主屏幕 / 安装应用

---

## 第一次打开要做两件事

**1. 导入历史。** 在电脑上跑

```
python tools/migrate.py <旧的 data.db> <旧的 config.yaml> -o backup.json
```

把 `backup.json` 传到手机(微信发给自己就行),
在 app 里「设置 → 导入备份」。

⚠️ 导入是**覆盖**,不是合并。它会先告诉你要盖掉什么,再让你确认。

**2. 补一笔组合外资产的金额。** 首页顶上那行提示点进去就是录入页,
「组合之外」那一段填一下 —— 旧库里从来没记过那个数,
所以现在的总额不含它。

---

## 数据在哪

**只在这台设备的浏览器里。** 没有账号、没有服务器、没有云。

意味着:**换手机、清缓存、清网站数据 → 全没了。**
「设置 → 导出备份」是目前唯一的兜底,录完几期就导一次。

(计划里有一个「私有仓库 + token 自动同步」的方案,还没做。
做完之后手机和电脑就通了,而且不用手动导。)

---

## 改了代码怎么更新

```
sh tools/commit.sh   # 会跑 check.sh,不过不给提交
git push
```

推上去一两分钟后手机上刷新就是新的。
Service Worker 对 `index.html` 是**先走网络**的,所以不会卡在旧版;
其余文件先给缓存里的、后台悄悄更新,下次打开生效。

---

## 不想推到线上的时候

```
python tools/serve.py
```

打印一个 `http://192.168.x.x:8000/`,手机连同一个 WiFi 打开。
改一行刷新就见效,适合调界面。

⚠️ 局域网是 http,**装不了 Service Worker**(浏览器只允许 https 或 localhost),
所以这条路没有离线能力,而且电脑关了就打不开。

---

## 仓库

| | | |
|---|---|---|
| `escapecat/balance` | public | 代码。**金额一分钱都不在里面** |
| `escapecat/portfolio` | private | 旧的飞书版,存档不动 |

真实数据在 `%TEMP%/pf/` 下面,不进版本库:

- `backup-keep.json` —— `tools/migrate.py` 迁出来的
- `expected.json` —— `tools/mkbaseline.js` 生成的测试基线(人工核对过)

`tools/check.sh` 第 5 条守卫盯着注释和夹具里的千分位数字 ——
因为泄露从来不是「提交了数据库」,是那些看着像文档的例子。
