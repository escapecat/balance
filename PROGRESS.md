# PROGRESS

**这个文件只记两种东西:和最初设计不一致的地方 · 需要裁决的地方。**
做完了什么看 git log 就行,不在这儿重复。

⚠️ **这个仓库是公开的,金额一分钱都不许写进来** —— 包括注释、测试夹具和这个文件。
真实数据在 `%TEMP%/pf/` 下面(`backup-keep.json` · `expected.json`),两个都不进版本库。
`tools/check.sh` 有一条守卫盯着。

---

## 已经偏离最初设计的地方

### 1. 缺口的基数改了(2026-08-09)

**原设计**(计划书第二节):

```
1. 投完后总额 T = 持仓 + 可投现金      ← 不是当前总额,否则永远差一口气
```

**实际改成**:`T = 组合总额`(持仓 + **全部**现金,不减 cashFloor)。

**为什么**:现金是第七个类别,它自己有目标(5%)。保底那笔钱还是你的资产,
只是不拿去买基金。从基数里抠掉的话,每一类的目标都跟着缩水 ——
按 cashFloor × 目标比例的量,而其中最小的那一类直接从清单上消失了
(缺口被压成负数,过滤掉了)。

更要命的是**同一屏上会出现两个数**:「配置」表走 `Portfolio.summarize`(基数是总额),
待办清单走 `Allocate.gaps`(基数减了 cashFloor),同一个类别给出两个缺口。
两个都言之凿凿地并排放着,你不知道该信哪个 —— 这比单纯算错更糟。

`tools/jstest/e2e.js` 现在有一条断言直接钉住「两处必须给出同一个数」。

**计划书里那几个示例数字是旧口径的产物**,没去改它。现在的正确值跑
`node tools/mkbaseline.js` 就能看到。

### 2. 组合外资产:两条路径合成一条(2026-08-09)

**原设计**没说清金额存哪。第一版写成两处都有:`assets[].value` 一份、
`snapshot.external` 一份。录入页只写后者,「现在」页只读前者 ——
于是填完之后,页面会一边把它算进总额,
一边挂着「还没填过金额,没算进上面这个数」。

**定死**:金额只在 `snapshot.external`,`assets[]` 只存名目(id/name/kind)。

顺带修了 `Ledger.delta` 把 external 算进组合总额的口径错误 ——
房产估值改一次,组合就凭空「涨」一次,
而 `delta` 存在的全部理由就是分清「赚的」和「投的」。

### 3. 「录入」的入口(2026-08-09)

**原设计**:过期(>25 天)时顶部换成整宽的「录这个月的数字」。

**问题**:录入不占 tab(它是动作不是地方),于是**刚录完的那 25 天里一个入口都没有** ——
想补一笔组合外资产、想改抄错的数字,整页没有一处能点。
`tools/view.js` 打出「可点 0」才发现的。

**改成**:按钮永远在,不过期时放在页尾、用次要样式。

### 4. Service Worker 的缓存名(2026-08-09)

`app/sw.js` 是从菜谱工具复制过来的,缓存名原样叫 `mealplanner`,
而且 activate 时会**删掉所有别的名字**的缓存。

CacheStorage 是按 origin 共享的,不看 scope —— 两个 PWA 都装在同一个
`*.github.io` 上的话,谁后激活谁就把对方的离线缓存全清了。
表现是「另一个 app 突然打不开了」,而你根本想不到是这边干的。

**两边都改了**:缓存名带项目前缀,清理时只清自己的。

---

## 试过但不做的

### 实时估值 —— 「打开时拉一次涨跌」(2026-08-10 验证后放弃)

想做的是:打开 app 时拉一次当天估值,提醒「哪一类今天跌多了,该加仓」。

**做不了,因为拿不到数据。** 命令行探测四个基金代码全部失败,分不清是
网络环境挡了还是接口变了,所以专门做了一页 `quote-test.html`
让浏览器实地试(浏览器的请求头、Referer、Cookie 环境和 curl 完全不同,
而最终跑这段代码的正是浏览器)—— 结果仍然不通。

**不写一个自己验证不了的功能。** 验证页已删。

如果哪天想重做:先把那一页恢复出来跑通,再谈功能。零依赖是硬要求,
所以只有 JSONP 这一条路(`<script>` 标签不受 CORS 限制),
而它依赖第三方保持那个固定的回调名 `jsonpgz` —— 这本来就是个脆弱的前提。

---

## 需要裁决的

### 007174 —— 一笔悬空引用,只在第一期出现过

基金清单里只有 `007147`(博时中债1-3年国开行A),看着像建仓那天的笔误
(数字一样,顺序不同)。

**现在的处理**:原样留着,设置页最上面挂着「未分类 · 1 只」,点一下就能归类。
不阻塞任何功能 —— 什么时候想起来什么时候点。

### 组合外资产还没填过金额

`assets[]` 里有一条 MSFT,但**旧库里从来没记过金额**。
所以现在总额不含它。

「现在」页顶部有一行提示说明这件事,点一下进录入页就能补。
计划书里提到的那个数是口述的,我没替你填进去 —— 那不是记录,是我编的。
